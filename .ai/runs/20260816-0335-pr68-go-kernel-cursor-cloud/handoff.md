# Handoff — PR #68 Ralph Go kernel

Go `ralph` owns observe/decide/dispatch/persist. Cursor preToolUse
invokes `go run ./ralph hook` fail-closed before implementation edits.
READY_FOR_HUMAN_REVIEW is the only success terminal. Cursor is not Saul.
Do not merge. Do not mark ready.
