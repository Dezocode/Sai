# Handoff — dashboard ICM scaffold

Added full instructional filesystem under `openclaw-dashboard/`:

- ICM-HANDBOOK.md, CONTEXT.md, docs/icm-protocol-handbook.md
- tabs/* and settings/* with CONTEXT.md + BUILD.md per surface
- services/, apps/, integrations/, host/, tests/smoke/ stubs
- contract.json → dashboard_icm block + updated deliverable paths

Alfred builds by reading each tab folder before implementing that surface.
