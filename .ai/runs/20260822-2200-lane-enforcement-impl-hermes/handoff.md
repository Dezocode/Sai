# Handoff - task 20260822-2200-lane-enforcement-impl-hermes
Status: in_progress until post-push CI + genuine exact-head Saul P0=P1=P2=0.
R1-R17 shipped (all Saul/Codex exact-head P1/P2 closed; R17: manifest walk-discovery gate + access-modified SwiftUI import lock @930a2e9; gofmt canonicalization at e1ce704). R18: budget restore — CI counts ALL added lines incl. .ai/runs (1250>1200); sanctioned condensation (table-row joins, comment joins, artifact trims) restores ≤1200 by the CI formula, zero vector-class loss.
R19 (Saul check 97279716328 @2ea00f2): P1 vendored/nested `prototypes` bypass closed — manifest walk prunes ONLY the canonical top-level lane tree (`p == Join(root,"prototypes")`), so third_party/prototypes-style trees stay inspected. P1 outside-symlink-alias bypass closed — moduleTargetsLane EvalSymlinks absolute replace targets and judges vs canonical root: alias into the lane fails closed, dangling stays lexical, real outside dirs/repo root legal. Vectors: vendored+nested lane refs FAIL, clean vendored PASS, alias FAIL, parent/root/`..` PASS.
