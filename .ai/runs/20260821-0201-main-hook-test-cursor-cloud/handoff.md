# Handoff — 20260821-0201-main-hook-test-cursor-cloud
PR 69 merge `f5f4fcb` failed `TestLinkedWorktreeHook` on main. Allow only exact BASE cache `sai-verify-$merge-base-$srcid`. Codex on `019fdbb` (codex_invoked=true) P1: the decoy glob was tautological. Plant `sai-verify-$HEAD-cafec0decafe0000`, remove the BASE bin, invoke the hook, require FEATURE CONTEXT, decoy stays 1 byte, and BASE srcid is rebuilt. GitHub "all checks completed without failures" on `019fdbb` is not SUCCESS (Saul ACTION_REQUIRED). Bugbot on #69: drive fails `scripts/agent-report` rows that omit `has=`. Keep PR 72 draft. Do not reopen #69.
