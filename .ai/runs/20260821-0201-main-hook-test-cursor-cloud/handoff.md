# Handoff — 20260821-0201-main-hook-test-cursor-cloud
PR 69 merge `f5f4fcb` failed `TestLinkedWorktreeHook` on main. Allow only exact BASE cache `sai-verify-$merge-base-$srcid` (wrapper srcid). Prefix `sai-verify-$merge-base-*` is not enough when merge-base equals HEAD. Plant decoy `sai-verify-$HEAD-cafec0decafe0000` and require the predicate to reject it. Bugbot on #69: drive now fails `scripts/agent-report` recipes that omit `has=` (pinned `runRecipe`/`err` bodies stay identical to BASE). Keep PR 72 draft. Do not reopen #69.
