# Handoff — 20260824-0235-pr77-ci-dedupe-ox-alpha

CI dedupe: push trigger ["**"]->[main] in agent-audit.yml + feature-maps-pages.yml; PR validation via pull_request; main keeps deploy+push-confirm. Post-push ad89aed shows single run per workflow.
