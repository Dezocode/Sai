# Plan — PR #15 P1 shell least-privilege

1. Remove wildcard `Bash(git *)` / `Bash(gh *)` from bootstrap and splunk-clone settings.
2. Document `shell_allowlist_after_phase_5b` with explicit allow/exclude lists.
3. Record contract amendment and onboarding guidance.
4. Verify hierarchy + audit; push to PR branch; post PR review + #agentupdates VERIFY.
