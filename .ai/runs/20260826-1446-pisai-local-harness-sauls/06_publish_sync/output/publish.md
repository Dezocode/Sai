# Publish

PR 1 is the contract/docs/test layer. The follow-up router PR is intentionally
stacked on this branch so the implementation cannot be interpreted without
its ownership and safety contract.
