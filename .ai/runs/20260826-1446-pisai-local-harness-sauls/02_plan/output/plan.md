# Plan

Create a prototype-only contract for a Mac-hosted PiS AI gateway. Define
model-selectable Pi hooks, exact environment variables, endpoint ownership,
telemetry evidence, memory boundaries, and a Mermaid topology. Follow-up PR
adds the reference router and mocked end-to-end proof.

Review gate: do not alter Sai production code, duplicate Mac inference on
Hostinger, upload weights, or claim runtime proof from contract tests.
