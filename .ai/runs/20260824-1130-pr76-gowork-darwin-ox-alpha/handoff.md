# Handoff 20260824-1130-pr76-gowork-darwin-ox-alpha
Fixed checkGoWorkFiles platform divergence: gwLane now canonicalizes candidates via the nearest-existing-ancestor scheme (same as resolvesInto) and judges against the symlink-resolved root by prefix, instead of filepath.Rel over mixed resolved/unresolved prefixes. Root cause of the macOS CI Sai Design Language failures (TestGoWorkUseIntoLaneFails et al): on macOS /var is a symlink to /private/var; when a go.work target's leaf did not exist EvalSymlinks kept the unresolved prefix and Rel silently missed the lane. Vectors unchanged: lane use/replace FAIL (all platforms), clean/outside PASS. Gates: build/vet/test green at HEAD, design-check PASS.
