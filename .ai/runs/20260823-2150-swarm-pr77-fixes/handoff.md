# Handoff — 20260823-2150-swarm-pr77-fixes (PR-77 swarm lane)

Restored the literal `go run ./cmd/sai-verify maps` needle in `load_kernel_maps` (dropped by the 05b15db docstring reword; caused the **Pages maps** drive-row FAIL fail=1/63 pass at HEAD 1503f2b, run 32664131109) and made `refreshChecks` paginate `/commits/<sha>/check-runs` to `total_count` via per-page ETag keys, capped at `CHECK_PAGES_MAX=3` budgeted requests; past the cap the card renders `partial` instead of silently dropping runs beyond page one (Saul P1, Codex 97255357944). Adversarial `--selftest` covers multi-page fetch, cross-page rerun clearing a page-one failure, page-two Saul failure gating, cap→partial, and 304 semantics; `--check` gained pagination needles. Comments/templates/selftest condensed to hold the budget. A concurrent lane writer's README docs, timezone-aware probe fixtures, and extra Saul-beyond-page-one fixture were verified coherent and folded in.
