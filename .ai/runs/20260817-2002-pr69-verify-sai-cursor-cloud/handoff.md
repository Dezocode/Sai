# Handoff — 20260817-2002-pr69-verify-sai-cursor-cloud
PR #69 `/goal` on `verification/repo-feature-map`. Keep draft; do not merge.
Kernel `cmd/sai-verify` is the only parser. Driving bullets are structured `::` recipes (`exec.CommandContext`); map text never reaches `bash -lc`. Hooks inject FEATURE CONTEXT (no `permission: allow`); deny on map/preserve/stale HEAD and on mutations with stale evidence. Completeness is exact-HEAD evidence whose `map_hash` covers watched implementation surfaces. Mutation `scripts/agent-report` recipes require a newly appearing queue file that contains `has=`. Stale-hook recovery is exactly `go run ./cmd/sai-verify drive` or `proof`; drive writes `--evidence` only under HEAD, gitdir, `os.TempDir()`, `/tmp/`, or `RUNNER_TEMP`. Mutations deny when evidence is unbound. Local hook builds BASE kernel when present and does not compile candidate Go during bootstrap. Bootstrap injects FEATURE CONTEXT and does not deny mutations (no trusted kernel yet). Receipts fail closed unless `repo`, BASE, HEAD, and `map_hash` are present and match. Flags that take a value error when the value is missing. Sweep matches git paths exactly or by explicit glob/brace prefix, never substring. Grouped/aliased `os/exec` imports fail closed. Trusted BASE policy evaluates candidate as data after merge. Cache uses `--absolute-git-dir`.
Unreachable: live gateway needs `openclaw` CLI; ingest/MCQ/session/run-all stubs (`expect=2`); connection-gate without registry evidence. Product gap: `.ai/projects/mimi-dispatcher/` absent. Sweep must stay unmapped=0. Saul Hostinger `go -version` sandbox failure is infra, not a product finding.
