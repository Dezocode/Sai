# Handoff — 20260817-2002-pr69-verify-sai-cursor-cloud
PR #69 `/goal` on `verification/repo-feature-map`. Keep draft; do not merge.
Kernel `cmd/sai-verify` is the only parser. `relevant` returns native title/why/subfeatures/entry_points/proofs/gotchas/paths plus verification. Hooks inject FEATURE CONTEXT (no `permission: allow`). Completeness is exact-HEAD evidence, not map parse.
Hooks omit `permission: allow`; deny on map/preserve/stale HEAD. Binary cache is `--absolute-git-dir`.
Live drive (named recipes): pass=59 fail=0 skip=0. Unreachable: live gateway needs `openclaw` CLI; ingest/MCQ/session/run-all stubs (exit 2); connection-gate without registry evidence. Product gap: `.ai/projects/mimi-dispatcher/` absent. Parent-folder agent files grouped; pstack slash catalog is marketplace. Source-wave leftovers named on existing bullets: Alfred `session-memory`/`BLOCKED-MCQ-CONTINUATION`, `secrets-store.schema.json`, `/verify-sai` + cold doctor/drive/preserve/hook, design smoke operator path, schema `BYPASS` vs reporting.yaml, `first-prompt-attach-contract.md`.
Do not claim 100% mapping unless a whole-repo source sweep finds zero unmapped concrete supported surfaces. 2026-08-18 sweep: unmapped supported surfaces 0; product gaps named. Cloud hooks omit sessionStart/End, MCP, workspaceOpen.
Next: after merge, BASE-built `sai-verify preserve` is the trusted anti-regression path. Cold-map/hook tests supply an explicit empty bootstrap BASE; missing `origin/main` no longer fails them. Deadbeef missing-base remains fail-closed.
