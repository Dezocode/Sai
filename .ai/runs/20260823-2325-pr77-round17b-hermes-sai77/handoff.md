# Handoff — Round-17b check-run pagination hardening (PR 77)
Saul P1 @ 106cc42: a page-level HTTP 304 made fetchAllChecks return mid-loop ({runs:null}), discarding earlier pages and keeping stale CHECKS[sha] silently. Fix: PAGE_CACHE stores last-known {list,total} per (sha,page); a 304 page reuses cache and CONTINUEs (aggregate rebuilds fully); no cached body -> honest {runs:null}. Largest total_count governs completeness (stale smaller cache total = partial, never fabricated). ghStepDue re-checked BETWEEN pages; gate stop breaks out labeled partial, bounded within the 60-request window budget. Adversarial --selftest replaced the bug-codifying vector: mixed 200/304 rebuilds aggregate, all-304-no-cache honest, stale totals partial, budget exhaustion stops after 1 request, between-page gate stops after page 2 keeping fetched runs. Local gates green (--check ALL PASS incl. prs-probe; emitted JS node-clean). Draft only; trailers conforming; no merge/ready/force-push.
