# Cursor automation profile — Alfred (delegated)

> Alfred is **OpenClaw-primary** (`openclaw-gateway-vps`). This root file satisfies
> ICM folder layout; the authoritative automation spec is:
>
> **`.ai/agents/alfred/runtimes/openclaw/automation/profile.md`**
>
> Do not create a Cursor Automation as Alfred's primary dispatch path.

See dezocode PR #45 review: OpenClaw agents use Gateway cron/webhooks + host CLI on VPS.
