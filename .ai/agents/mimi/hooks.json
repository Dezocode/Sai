{
  "agent_id": "mimi",
  "git_hooks": {
    "path": ".githooks",
    "install": "scripts/install-agent-hooks",
    "hooks": ["post-checkout", "post-commit", "post-merge", "post-rewrite", "pre-push", "post-push-equivalent.sh"]
  },
  "cursor_rules": [".cursor/rules/sai-coordination.mdc"],
  "reporting_channels": {
    "agentupdates": "C0BH15HDN2Z",
    "help_newagents": "C0BH8LCJLDS"
  },
  "automation_triggers": {
    "required": [
      {
        "type": "scheduled",
        "status": "active",
        "mechanism": "Claude Code scheduled task (cron), not a Cursor Automation",
        "task_id": "mimi-protocol-upkeep",
        "schedule": "0 9 * * * (09:01 local, daily)",
        "created": "2026-07-14",
        "caveat": "Runs only while the Claude app is open at fire time; if closed, it runs on next launch instead. This is not an always-on cloud cron.",
        "note": "baseline protocol upkeep: flush queue, verify audit + semantic hierarchy, agent-sync-drive, fork CI drift check, post [SAI][VERIFY] or [SAI][BLOCKED] to #agentupdates"
      }
    ],
    "optional": [
      {
        "type": "slack-polling",
        "trigger": "@mimi mention in SAI public channels, detected by 15-min poll",
        "status": "active",
        "mechanism": "Claude Code scheduled task 'mimi-slack-dispatch-poll' (cron */15 * * * *), created 2026-07-17; loads the full Mimi profile mirror, scans public channels for new @mimi mentions, replies in-thread per ICM protocol with ID-matrix tag line",
        "caveat": "Polling, not event-driven: worst-case ~15 min latency, and runs only while the Claude app is open at fire time. First-response evidence pending; promote to 'verified' only with a real mention->reply message-link pair.",
        "id_matrix": ".ai/agents/mimi/docs/slack-id-matrix.md"
      },
      {
        "type": "slack",
        "trigger": "@mimi mention in #agentupdates or authorized project channels (event-driven)",
        "status": "building",
        "promotion_rule": "status may become 'verified' only with an evidence URL or log showing a real @mimi mention producing a real dispatch run (Slack message link + resulting .ai/runs/<task-id>/)",
        "design": ".ai/agents/mimi/docs/dispatch-triggers.md (Slack Events API / Socket Mode bridge)",
        "note": "True inbound-event trigger still requires the documented OSS bridge; the polling task above is the interim implementation. Not claimed as working."
      },
      {
        "type": "github",
        "trigger": "@mimi in issue/PR comments, or labeled issues, on monaecode/Sai and Dezocode/Sai",
        "status": "building",
        "promotion_rule": "status may become 'verified' only with an evidence URL or log showing a real GitHub event producing a real dispatch run (comment/Action run link + resulting .ai/runs/<task-id>/)",
        "design": ".ai/agents/mimi/docs/dispatch-triggers.md; stub workflow .github/workflows/mimi-dispatch-stub.yml (workflow_dispatch only, no secrets)",
        "note": "Stub proves plumbing shape only; end-to-end trigger not built. Not claimed as working."
      }
    ]
  },
  "ci_enforcement": {
    "workflow": ".github/workflows/agent-audit.yml",
    "verifiers": [
      "scripts/verify-agent-audit",
      "scripts/verify-semantic-hierarchy",
      "scripts/verify-merge-handoff"
    ],
    "merge_handoff": "scripts/ci-merge-handoff-slack",
    "icm_reference": "https://arxiv.org/abs/2603.16021"
  }
}
