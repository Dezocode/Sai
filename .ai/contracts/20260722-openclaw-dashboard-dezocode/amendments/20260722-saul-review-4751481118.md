# Amendment: Saul CTO re-review fixes (4751481118)

| Field | Value |
|---|---|
| **Date** | 2026-07-22 |
| **Review** | https://github.com/Dezocode/Sai/pull/45#pullrequestreview-4751481118 |
| **Applied by** | Cora (ctr-admin) |

## P1-A — Gateway loopback (inline on gateway-options.json)

**Finding:** Default `0.0.0.0` bind with Slack/Telegram enabled.

**Fix:**
- `gateway-options.json` → `host: 127.0.0.1`, `bind_policy: loopback_default`
- `dashboard_ingest.bind: 127.0.0.1`
- `verify-gateway-bind.sh` fails if `0.0.0.0` reintroduced
- CI step in `agent-audit.yml`

## P1-B — Subagent gate fail-open (inline on subagent-connection-gate.sh)

**Finding:** Gate could pass on document/header only.

**Fix:**
- `subagent-connection-gate.sh` runs `--self-test` then `verify-agent-telegram.sh --scope contract`
- No stub PASS path; `all-gates.sh` runs negative regression in CI path
- Full enforce gate remains fulfillment-only until registry populated

## P1-C — Placeholder acceptance in verify-agent-telegram.sh

**Finding:** `@` and `http-not-a-url` passed validators.

**Fix:**
- Strict regex: `https://t.me/...` or `https://telegram.me/...`
- Slack: `https://*.slack.com/archives/.../p...` only
- `habbo_presence` must be exactly `connected` (or BLOCKED row)
- `--self-test` negative regression; CI enforced

## P2 — Alfred AGENT.md wrong tools.json pointer

**Finding:** Pointed to `runtimes/cursor/tools.json`.

**Fix:** `runtimes/openclaw/tools.json` (canonical OpenClaw-primary)

## Re-review request

After merge of this commit, Saul may re-review for CTO approval.
