# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently a **greenfield skeleton** for **Sai** (parental-guided safe internet/AI access for children). As of the latest `main`, the only tracked content is `README.md` and `Team.md` — there is **no application source**, package manifest, Docker Compose stack, lint/test scripts, or runnable service.

Until application code is added:

- There is nothing to install beyond what the cloud VM already provides (Node/npm/pnpm and Python are available for future work).
- There is no lint, test, build, or `dev` command to run.
- Do not invent a stack or scaffold an app during environment-setup sessions unless the user explicitly asks for product implementation.

When a real app lands, update this section with service startup caveats, ports, and pointers to README/`package.json` (or equivalent) scripts rather than duplicating standard commands here.
