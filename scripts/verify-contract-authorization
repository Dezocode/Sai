#!/usr/bin/env bash
# verify-contract-authorization — revision binding, dual approval, human gate.
set -euo pipefail
ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
exec python3 "$ROOT/scripts/lib/sai_auth_verify.py" --contract "$@"
