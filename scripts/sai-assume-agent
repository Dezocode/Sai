#!/usr/bin/env bash
# sai-assume-agent — verified identity transition. Cannot assume Saul on Cursor.
set -euo pipefail
ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
exec python3 "$ROOT/scripts/lib/sai_auth_flow.py" assume "$@"
