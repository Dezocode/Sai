#!/usr/bin/env bash
# sai-event-adapter — digest-compare GitHub/Saul payloads; NOOP duplicates.
set -euo pipefail
ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
exec python3 "$ROOT/scripts/lib/sai_auth_event.py" "$@"
