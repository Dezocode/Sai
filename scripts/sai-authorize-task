#!/usr/bin/env bash
# sai-authorize-task — resolve implementation identity or raise CONTRACT_REQUIRED.
set -euo pipefail
ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
exec python3 "$ROOT/scripts/lib/sai_auth_flow.py" authorize "$@"
