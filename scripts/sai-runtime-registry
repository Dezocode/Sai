#!/usr/bin/env bash
# sai-runtime-registry — named worker identity (not task titles).
# referenced: scripts/lib/sai_auth_runtime.py scripts/lib/sai_auth_runtime_test.py
set -euo pipefail
ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
exec python3 "$ROOT/scripts/lib/sai_auth_runtime.py" "$@"
