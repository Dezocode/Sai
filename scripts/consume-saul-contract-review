#!/usr/bin/env bash
# consume-saul-contract-review — Cora-only. Maps CTO findings to amendments.
set -euo pipefail
ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd)
exec python3 "$ROOT/scripts/lib/sai_auth_review.py" consume "$@"
