---
description: Ralph Go kernel owns edit steering. Rules do not authorize edits.
alwaysApply: true
---
Edits: `go run ./ralph hook`. Consume `Dispatch.work_item`. Request validation; never wait on it. Env is not Grant.
