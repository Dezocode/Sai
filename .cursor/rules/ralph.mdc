---
description: Ralph Go kernel owns edit steering. Rules do not authorize edits.
alwaysApply: true
---
Edits: `go run ./ralph hook`. Consume current `Dispatch.work_item` from `go run ./ralph reconcile --once`. Env is not Grant.
