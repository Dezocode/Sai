---
description: Ralph Go kernel owns edit steering. Rules do not authorize edits.
alwaysApply: true
---
Edits: `go run ./ralph hook`. Long-lived: `go run ./ralph run`. Wake: `go run ./ralph reconcile --once`. Worker: `go run ./ralph worker-result`. Env is not Grant.
