/// Namespace for the shared native design authority.
/// Concrete tokens/components are added during the design phase, before feature UI is unlocked.
public enum SaiDesignLanguage {
    public static let contractVersion = 1
}
