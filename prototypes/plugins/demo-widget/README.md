# demo-widget

Harness fixture for Foundry slice 80 spin-off planner tests.

The graph exports `src/widget.py`, treats `widget-api` as a REMOTE contract,
and drops `src/legacy.py` from the candidate tree.
