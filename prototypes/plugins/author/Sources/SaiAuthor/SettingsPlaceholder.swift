import SaiDesignLanguage
import SwiftUI

public struct SettingsPlaceholder: View {
    public init() {}

    public var body: some View {
        SaiCanvas {
            SaiText("Settings placeholder")
        }
    }
}
