#!/usr/bin/env bash
# foundry-spinoff — slice 80 spin-off planner CLI.
# git-index-mode: 100755
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${ROOT}:${PYTHONPATH:-}"
exec python3 -m spinoff "$@"
