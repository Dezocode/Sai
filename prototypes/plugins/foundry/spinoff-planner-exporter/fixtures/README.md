# Slice 78 graph schema fixture

This JSON schema is a pinned fixture copy of the slice 78 Foundry graph
contract. It exists here so slice 80 tests and tooling can validate
fixture-shaped graphs until slice 78 (#167) merges to `main`.
